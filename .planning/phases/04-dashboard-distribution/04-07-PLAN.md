---
phase: 04-dashboard-distribution
plan: 07
type: execute
wave: 1
depends_on: []
files_modified: [glama.json, npx/package.json, wrappers/langchain/pyproject.toml, wrappers/crewai/pyproject.toml, README.md]
autonomous: false
gap_closure: true
requirements: [DIST-04, DIST-06, DIST-07, DIST-08]

must_haves:
  truths:
    - "glama.json contains the real GitHub username (AmirK-S), not a placeholder"
    - "All pyproject.toml and package.json files reference the real GitHub repo URL, not your-org placeholders"
    - "hivemind-langchain is published to PyPI and installable via pip install hivemind-langchain"
    - "hivemind-crewai is published to PyPI and installable via pip install hivemind-crewai"
    - "MCP directory submissions are initiated for at least 4 directories"
  artifacts:
    - path: "glama.json"
      provides: "Glama.ai auto-indexing config with real maintainer"
      contains: "AmirK-S"
    - path: "wrappers/langchain/pyproject.toml"
      provides: "LangChain package config with real repo URL"
      contains: "AmirK-S"
    - path: "wrappers/crewai/pyproject.toml"
      provides: "CrewAI package config with real repo URL"
      contains: "AmirK-S"
    - path: "npx/package.json"
      provides: "npx package config with real repo URL"
      contains: "AmirK-S"
  key_links:
    - from: "glama.json"
      to: "Glama.ai directory"
      via: "auto-indexing on push to main"
      pattern: "AmirK-S"
---

<objective>
Close Gap 1 from verification: Fix all placeholder values in distribution configs and publish framework wrappers to PyPI, then initiate MCP directory submissions.

Purpose: Success Criterion 4 requires framework wrappers published to registries and HiveMind listed in at least 4 MCP directories. Currently all URLs contain "your-org" placeholders, glama.json has "your-github-username", and neither PyPI package is published.
Output: Published PyPI packages, corrected config files, and directory submissions initiated.
</objective>

<execution_context>
@/Users/amirkellousidhoum/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amirkellousidhoum/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dashboard-distribution/04-05-SUMMARY.md
@.planning/phases/04-dashboard-distribution/04-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix all placeholder values in distribution configs</name>
  <files>glama.json, npx/package.json, wrappers/langchain/pyproject.toml, wrappers/crewai/pyproject.toml, README.md</files>
  <action>
    Update all placeholder values with the real GitHub username and repo URL:

    1. **glama.json**: Change `"maintainers": ["your-github-username"]` to `"maintainers": ["AmirK-S"]`

    2. **npx/package.json**: Change `"url": "https://github.com/your-org/hivemind"` to `"url": "https://github.com/AmirK-S/HiveMind"`

    3. **wrappers/langchain/pyproject.toml**: Change `Homepage = "https://github.com/your-org/hivemind"` to `Homepage = "https://github.com/AmirK-S/HiveMind"`

    4. **wrappers/crewai/pyproject.toml**: Change `Homepage = "https://github.com/your-org/hivemind"` to `Homepage = "https://github.com/AmirK-S/HiveMind"`

    5. **README.md**: Change `git clone https://github.com/your-org/hivemind.git` to `git clone https://github.com/AmirK-S/HiveMind.git`

    The GitHub username "AmirK-S" is from the git config email `37595327+AmirK-S@users.noreply.github.com`. The repo name is "HiveMind" (matching the working directory name, case-sensitive).
  </action>
  <verify>
    Run: `grep -r "your-org\|your-github-username" glama.json npx/package.json wrappers/langchain/pyproject.toml wrappers/crewai/pyproject.toml README.md`
    Expected: No matches (all placeholders replaced).
    Run: `grep "AmirK-S" glama.json npx/package.json wrappers/langchain/pyproject.toml wrappers/crewai/pyproject.toml`
    Expected: All four files contain "AmirK-S".
  </verify>
  <done>All distribution config files reference the real GitHub username and repo URL. No "your-org" or "your-github-username" placeholders remain in any distribution-related file.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Publish framework wrappers to PyPI and submit to MCP directories</name>
  <files>N/A (external submissions and PyPI publishing)</files>
  <action>
All distribution config placeholders have been fixed. The user must now:

**PyPI Publishing (DIST-07, DIST-08):**

1. Publish hivemind-langchain:
   ```bash
   cd wrappers/langchain
   pip install build twine
   python -m build
   twine upload dist/*
   ```

2. Publish hivemind-crewai:
   ```bash
   cd wrappers/crewai
   pip install build twine
   python -m build
   twine upload dist/*
   ```

**MCP Directory Submissions (DIST-04, DIST-06) — submit to at least 4 of 7:**

3. **Glama.ai**: Push repo to GitHub as `AmirK-S/HiveMind`, then claim at https://glama.ai/mcp/servers (auto-indexed via glama.json)
4. **PulseMCP**: Submit at https://pulsemcp.com/submit — name: "HiveMind", description: "Shared knowledge commons for AI agents via MCP", GitHub URL
5. **punkpeye/awesome-mcp-servers**: Open PR at https://github.com/punkpeye/awesome-mcp-servers following CONTRIBUTING.md
6. **Official MCP Registry**: Open PR at https://github.com/modelcontextprotocol/registry following CONTRIBUTING.md
7. **Smithery**: Run `npx smithery mcp publish "https://your-hivemind-instance.com/mcp"` (requires public HTTPS endpoint)
8. **mcp.so**: Submit via https://mcp.so or their GitHub
9. **AwesomeClaude.ai**: Submit via https://awesomeclaude.ai form

After completing at least 4 submissions, update the README MCP Directory Listings table status from "Pending submission" to the actual status (e.g., "Listed", "PR submitted", "Under review").
  </action>
  <verify>
`pip install hivemind-langchain` succeeds from a clean environment. `pip install hivemind-crewai` succeeds from a clean environment. At least 4 MCP directory submission confirmations or URLs available.
  </verify>
  <done>hivemind-langchain and hivemind-crewai are published to PyPI and installable. At least 4 MCP directory submissions are initiated. README MCP Directory Listings table is updated with actual statuses.</done>
</task>

</tasks>

<verification>
1. `grep -r "your-org\|your-github-username" glama.json npx/package.json wrappers/ README.md` returns no matches
2. `grep "AmirK-S" glama.json npx/package.json wrappers/langchain/pyproject.toml wrappers/crewai/pyproject.toml` returns matches in all 4 files
3. PyPI packages `hivemind-langchain` and `hivemind-crewai` are installable (after human action)
4. At least 4 MCP directory submissions initiated (after human action)
</verification>

<success_criteria>
- All placeholder values replaced with real GitHub username/repo URL
- hivemind-langchain published to PyPI
- hivemind-crewai published to PyPI
- At least 4 MCP directory submissions initiated
- README MCP Directory Listings table updated with actual statuses
</success_criteria>

<output>
After completion, create `.planning/phases/04-dashboard-distribution/04-07-SUMMARY.md`
</output>
